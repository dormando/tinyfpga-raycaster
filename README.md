See: http://www.dormando.me/post/fpga-raycaster/ for full detail.

Building:

- install icestorm. run `./build.sh`
- some lookup tables and textures are generated via scripts under the
  `scripts/` directory.
